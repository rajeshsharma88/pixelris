import React from "react";
import AppBar from "@material-ui/core/AppBar";
import ToolBar from "@material-ui/core/Toolbar";

const Header = (props) => {
  return;
  <AppBar>
    <ToolBar>Pixelris</ToolBar>
  </AppBar>;
};

export default Header;
